Basta adicionar a palavra npx e o sufixo -cli em todos os comandos.

Para fazer o login, digite:

Bash
npx eas-cli login
Para configurar o projeto:

Bash
npx eas-cli build:configure
Para gerar o aplicativo (APK):

Bash
npx eas-cli build -p android --profile preview